pillow>=10.3.0

# Breeze TTS 2 nodes (ITL Breeze TTS *). The model code itself ships inside the pack
# (vendor/breeze-tts, our fork of breezeblue-ai/breeze-tts; see vendor/breeze-tts/VENDORED.md),
# so nothing here points at GitHub: the Comfy registry scanner rejects URL dependencies.
# torch is deliberately NOT listed, and nothing below may carry a torch floor: on Windows PyPI only
# has CPU-only torch wheels, so any floor makes pip replace the host's CUDA torch
# (tests/test_requirements.py guards this). breeze_models checks torch >= 2.7 at import instead.
transformers>=4.57,<6
numpy>=1.25
librosa>=0.10
soundfile>=0.13
huggingface_hub>=0.25

# dev/test only (not needed at runtime):
# pytest>=8.0
